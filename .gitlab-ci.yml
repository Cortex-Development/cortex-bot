image: openjdk:15.0.1-jdk-buster

stages:
  - build
  - deploy

before_script:
  - chmod +x mvnw

build:
  stage: build
  script: ./mvnw package
  artifacts:
    paths:
      - target/cortex-bot-0.0.1-SNAPSHOT.jar

production:
  stage: deploy
  script:
    -
  only:
    - master